const bolsa = [
    'notebook',
    'moletom',
    'Escova de dente',
    'Celular',
    'Carregador',
    'Mouse',
]

for(let i = 0; i < bolsa.length; i++){
    console.log(bolsa[i])
 if (bolsa [i] == 'Carteira') break
}

